import NewItem from "./new-item";
export default function Page() {
    return (
        <main>
            <body className = "bg-black">
                <NewItem />
            </body>
        </main>
    );
}